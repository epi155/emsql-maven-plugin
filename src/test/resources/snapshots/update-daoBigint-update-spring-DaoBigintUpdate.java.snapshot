/*
 * Code Generated at 2026-04-09T12:05:48.503254Z
 * Plugin: io.github.epi155:emsql-maven-plugin:1.1-A3-SNAPSHOT
 */
package com.example.emsql;

import io.github.epi155.emsql.runtime.EmSQL;
import io.github.epi155.emsql.runtime.SqlArg;
import io.github.epi155.emsql.runtime.SqlTrace;
import java.sql.*;
import javax.sql.DataSource;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.jdbc.datasource.DataSourceUtils;
import org.springframework.stereotype.Repository;
import org.springframework.transaction.annotation.Transactional;

@Repository
public class DaoBigintUpdate {
    private static final org.slf4j.Logger log = org.slf4j.LoggerFactory.getLogger(DaoBigintUpdate.class);
    private final DataSource dataSource;
    @Autowired
    public DaoBigintUpdate(DataSource dataSource) {
        this.dataSource = dataSource;
    }

    private static final String Q_0001 = "UPDATE t_bigint SET bigint_null = ?, bigint_not_null = ? WHERE id = ?";
    /**
     * Template SqlUpdate
     * <pre>
     * update t_bigint set bigint_null = :bigintNull, bigint_not_null = :bigintNotNull where id = :id
     * </pre>
     *
     * @param bigintNull :bigintNull (parameter #1)
     * @param bigintNotNull :bigintNotNull (parameter #2)
     * @param id :id (parameter #3)
     * @throws SQLException SQL error
     */
    @Transactional
    public int update(            final Long bigintNull,
            final long bigintNotNull,
            final int id)
            throws SQLException {
        final Connection c = DataSourceUtils.getConnection(dataSource);
        if (log.isDebugEnabled()) {
            SqlTrace.showQuery(Q_0001, () -> {
                return new SqlArg[]{
                    new SqlArg("bigintNull", "Long", bigintNull),
                    new SqlArg("bigintNotNull", "long", bigintNotNull),
                    new SqlArg("id", "int", id)
                };
            });
        }
        try (PreparedStatement ps = c.prepareStatement(Q_0001)) {
            int ki=0;
            EmSQL.setLong(ps, ++ki, bigintNull);
            ps.setLong(++ki, bigintNotNull);
            ps.setInt(++ki, id);
            if (log.isDebugEnabled()) {
                SqlTrace.showQuery(Q_0001, () -> {
                    return new SqlArg[]{
                        new SqlArg("bigintNull", "Long", bigintNull),
                        new SqlArg("bigintNotNull", "long", bigintNotNull),
                        new SqlArg("id", "int", id)
                    };
                });
            }
            return ps.executeUpdate();
        } catch (SQLException e) {
            SqlTrace.showCause(e, Q_0001, () -> {
                return new SqlArg[]{
                    new SqlArg("bigintNull", "Long", bigintNull),
                    new SqlArg("bigintNotNull", "long", bigintNotNull),
                    new SqlArg("id", "int", id)
                };
            });
            throw e;
        }
    }
}
