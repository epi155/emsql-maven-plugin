/*
 * Code Generated at 2026-04-17T10:02:14.409539Z
 * Plugin: io.github.epi155:emsql-maven-plugin:1.1-A4-SNAPSHOT
 */
package com.example.emsql;

import io.github.epi155.emsql.runtime.EmSQL;
import io.github.epi155.emsql.runtime.SqlArg;
import io.github.epi155.emsql.runtime.SqlTrace;
import java.sql.*;
import javax.sql.DataSource;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.jdbc.datasource.DataSourceUtils;
import org.springframework.stereotype.Repository;
import org.springframework.transaction.annotation.Transactional;

@Repository
public class DaoBigintInsert {
    private static final org.slf4j.Logger log = org.slf4j.LoggerFactory.getLogger(DaoBigintInsert.class);
    private final DataSource dataSource;
    @Autowired
    public DaoBigintInsert(DataSource dataSource) {
        this.dataSource = dataSource;
    }

    private static final String Q_0001 = "INSERT INTO t_bigint ( bigint_null, bigint_not_null ) VALUES ( ?, ? )";
    /**
     * Template SqlInsert
     * <pre>
     * insert into t_bigint (bigint_null, bigint_not_null)
     * values (:bigintNull, :bigintNotNull)
     * </pre>
     *
     * @param bigintNull :bigintNull (parameter #1)
     * @param bigintNotNull :bigintNotNull (parameter #2)
     * @throws SQLException SQL error
     */
    @Transactional
    public int insert(            final Long bigintNull,
            final long bigintNotNull)
            throws SQLException {
        final Connection c = DataSourceUtils.getConnection(dataSource);
        if (log.isDebugEnabled()) {
            SqlTrace.showQuery(Q_0001, () -> {
                return new SqlArg[]{
                    new SqlArg("bigintNull", "Long", bigintNull),
                    new SqlArg("bigintNotNull", "long", bigintNotNull)
                };
            });
        }
        try (PreparedStatement ps = c.prepareStatement(Q_0001)) {
            int ki=0;
            EmSQL.setLong(ps, ++ki, bigintNull);
            ps.setLong(++ki, bigintNotNull);
            if (log.isDebugEnabled()) {
                SqlTrace.showQuery(Q_0001, () -> {
                    return new SqlArg[]{
                        new SqlArg("bigintNull", "Long", bigintNull),
                        new SqlArg("bigintNotNull", "long", bigintNotNull)
                    };
                });
            }
            return ps.executeUpdate();
        } catch (SQLException e) {
            SqlTrace.showCause(e, Q_0001, () -> {
                return new SqlArg[]{
                    new SqlArg("bigintNull", "Long", bigintNull),
                    new SqlArg("bigintNotNull", "long", bigintNotNull)
                };
            });
            throw e;
        }
    }
}
