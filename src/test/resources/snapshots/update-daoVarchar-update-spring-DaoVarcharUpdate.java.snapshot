/*
 * Code Generated at 2026-04-17T10:02:17.330040Z
 * Plugin: io.github.epi155:emsql-maven-plugin:1.1-A3-SNAPSHOT
 */
package com.example.emsql;

import io.github.epi155.emsql.runtime.EmSQL;
import io.github.epi155.emsql.runtime.SqlArg;
import io.github.epi155.emsql.runtime.SqlTrace;
import java.sql.*;
import javax.sql.DataSource;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.jdbc.datasource.DataSourceUtils;
import org.springframework.stereotype.Repository;
import org.springframework.transaction.annotation.Transactional;

@Repository
public class DaoVarcharUpdate {
    private static final org.slf4j.Logger log = org.slf4j.LoggerFactory.getLogger(DaoVarcharUpdate.class);
    private final DataSource dataSource;
    @Autowired
    public DaoVarcharUpdate(DataSource dataSource) {
        this.dataSource = dataSource;
    }

    private static final String Q_0001 = "UPDATE t_varchar SET vc_null = ?, vc_not_null = ? WHERE id = ?";
    /**
     * Template SqlUpdate
     * <pre>
     * update t_varchar set vc_null = :vcNull, vc_not_null = :vcNotNull where id = :id
     * </pre>
     *
     * @param vcNull :vcNull (parameter #1)
     * @param vcNotNull :vcNotNull (parameter #2)
     * @param id :id (parameter #3)
     * @throws SQLException SQL error
     */
    @Transactional
    public int update(            final String vcNull,
            final String vcNotNull,
            final int id)
            throws SQLException {
        final Connection c = DataSourceUtils.getConnection(dataSource);
        if (log.isDebugEnabled()) {
            SqlTrace.showQuery(Q_0001, () -> {
                return new SqlArg[]{
                    new SqlArg("vcNull", "String", vcNull),
                    new SqlArg("vcNotNull", "String", vcNotNull),
                    new SqlArg("id", "int", id)
                };
            });
        }
        try (PreparedStatement ps = c.prepareStatement(Q_0001)) {
            int ki=0;
            EmSQL.setVarchar(ps, ++ki, vcNull);
            ps.setString(++ki, vcNotNull);
            ps.setInt(++ki, id);
            if (log.isDebugEnabled()) {
                SqlTrace.showQuery(Q_0001, () -> {
                    return new SqlArg[]{
                        new SqlArg("vcNull", "String", vcNull),
                        new SqlArg("vcNotNull", "String", vcNotNull),
                        new SqlArg("id", "int", id)
                    };
                });
            }
            return ps.executeUpdate();
        } catch (SQLException e) {
            SqlTrace.showCause(e, Q_0001, () -> {
                return new SqlArg[]{
                    new SqlArg("vcNull", "String", vcNull),
                    new SqlArg("vcNotNull", "String", vcNotNull),
                    new SqlArg("id", "int", id)
                };
            });
            throw e;
        }
    }
}
