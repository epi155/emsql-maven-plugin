<project>
    <modelVersion>4.0.0</modelVersion>
    <groupId>test</groupId>
    <artifactId>test-type-model</artifactId>
    <version>1.1-A4-SNAPSHOT</version>

    <build>
        <plugins>
            <plugin>
                <groupId>io.github.epi155</groupId>
                <artifactId>emsql-maven-plugin</artifactId>
                <version>${version}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <configDirectory>${project.basedir}/src/test/resources</configDirectory>
                    <generateDirectory>${project.basedir}/target/generated-test-sources/emsql-tm</generateDirectory>
                    <debugCode>true</debugCode>
                    <autoPad>true</autoPad>
                    <modules>
                        <module>typeModel.yaml</module>
                    </modules>
                    <addCompileSourceRoot>false</addCompileSourceRoot>
                    <addTestCompileSourceRoot>false</addTestCompileSourceRoot>
                    <parserProvider>JSQL</parserProvider>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>

